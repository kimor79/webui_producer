webui_producer requires the following:

* PHP (tested with 5.3)
* YUI 2 (tested with 2.9 hosted on Yahoo!)
* A webserver (tested with Apache 2.2)

Copy the files in php/ to a path in php's include_path. E.g.:
  cp -R php /usr/local/include/php/webui_producer

Copy the files in www/ to a path accessible by the webserver. E.g.:
  cp -R www /usr/local/www/webui_producer
